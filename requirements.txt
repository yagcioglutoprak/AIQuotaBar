rumps>=0.4.0,<1.0
curl_cffi>=0.7.0,<1.0
browser-cookie3>=0.19.1,<1.0
# Required by the floating usage panel (Quartz.CGColor*). rumps only pulls
# pyobjc-framework-Cocoa, so Quartz must be declared explicitly.
pyobjc-framework-Quartz>=10.0
