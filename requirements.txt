rumps>=0.4.0,<1.0
curl_cffi>=0.7.0,<1.0
browser-cookie3>=0.19.1,<1.0
# The panel, settings and history windows render in a native WKWebView
# (system WebKit — not Electron). rumps only pulls pyobjc-framework-Cocoa.
pyobjc-framework-WebKit>=10.0
